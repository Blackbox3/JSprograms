let name = `Anusha`
let surName = `Shrestha`

let fullName = `My name is ${name} and surname is ${surName}` //we cannot call the variable inside ("") and ('') but can be called using backticks
console.log(fullName)