// Ireturn and change original array
// In this it doesn't support numeric sort 
// sort is mainly for string propose
// accending sort is done while descending is done by doing first of all ascending and reverse it
let studentName = ["anusha", "bikash", "ranjana", "arunita", "aayusha", "milan"];
let sortName = studentName.sort();
console.log(sortName);
